{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "lldb", // 使用LLDB
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}/build" // 设置当前工作目录为build目录
        }
    ]
}